{
  "name": "jimmillerdrums-email-forwarder",
  "version": "1.0.0",
  "description": "AWS Lambda function for forwarding emails via SES",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["aws", "lambda", "ses", "email"],
  "author": "Jim Miller",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  }
}
